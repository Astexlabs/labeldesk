hard rules for code style here:

-: no comments in code unless rly needed for some tradeoff, dont add unless u must
-: use lowercases for file names wit dashes (-), vars n funcs with camelCase only
-: every working code part needs good unittttests, no skipping this
-: code vibe has to be like v talented but junior dev, clear u still figuring some bits
-: varnames n code words shud be short (abbrev) n casual, dont spell out big wrds unless short
-: always keep code as small as poss, working > pretty looks or sugar. simple always wins
-: code has 2 show ur thoughts/steps as u go, make sense by itself not by explaing
-: doc strs + error/succ msg = super quick/human, no big explain, no formal